# Super Mario World 1-1 H5 关卡设计经验总结

## 项目概述

基于 HTML5 Canvas + 原生 JavaScript 实现的 Super Mario World 1-1 关卡复刻，支持在浏览器中运行，兼容 `file://` 协议（无需模块打包）。

### 技术栈
- HTML5 Canvas 渲染
- 原生 JavaScript（`<script>` 标签引入，非 ES Module）
- sharp（Node.js 图片处理库，用于精灵图预处理）
- Playwright（自动化测试截图验证）

---

## 核心架构设计

### 1. 双层地图数据结构

项目中存在两套并行的地图数据结构，各有用途：

| 数据结构 | 类型 | 用途 |
|---------|------|------|
| `levelMap[row][col]` | 数字矩阵 | 渲染层直接读取，存储瓦片类型编号 |
| `tiles[]` | 对象数组 | 物理碰撞检测，每个元素含 `{x, y, width, height, type}` |

**经验教训**：修改瓦片外观时（如问号箱被顶后变色），必须同时更新 `levelMap` 和 `tiles` 数组中的 `type`，否则渲染层和物理层会不一致。

```javascript
// 正确做法：同时更新两层数据
tile.type = 8;  // tiles 数组中的对象
this.levelMap[row][col] = 8;  // levelMap 数字矩阵
```

### 2. 游戏状态机

```
PLAYING ⇄ PAUSED
   ↓         ↑
  DEAD   (按 P 切换)
   ↓
FLAGPOLE → LEVEL_TRANSITION → PLAYING (下一关)
   ↓
  WIN
```

- 使用 `GameState` 类管理状态常量（`PLAYING=0, WIN=1, DEAD=2, FLAGPOLE=3, LEVEL_TRANSITION=4, PAUSED=5`）
- `update()` 方法中按状态分支处理，注意**早期 return 会阻止后续状态代码执行**
- `PAUSED` 状态：冻结物理更新，但渲染继续绘制（用于显示半透明遮罩 + PAUSED 文字）
- `LEVEL_TRANSITION` 状态：通关后短暂停留，自动加载下一关

**经验教训**：新增 `FLAGPOLE` 状态时，动画逻辑被 `if (state !== PLAYING) return` 提前拦截，导致动画不执行。正确做法是把新状态的处理逻辑放在早期 return 之前。

### 3. 渲染策略：Canvas 绘制 vs 精灵图

| 元素 | 方式 | 原因 |
|------|------|------|
| 地面、砖块、问号箱、已用箱、楼梯 | Canvas 代码绘制 | 避免精灵图边框导致的瓦片间隙 |
| 管道 | Canvas 代码绘制 | 管道顶部+身体分段绘制，保证无缝连接 |
| 金币 | Canvas 代码绘制 | 精灵图太小（16x20），放大后模糊 |
| Mario、板栗仔 | 精灵图 | 角色形状复杂，Canvas 绘制效果差 |

**核心经验**：AI 生成的精灵图几乎都带有黑色描边，当多个瓦片拼接时会产生明显的网格线。对于简单的方形瓦片，Canvas 代码绘制比精灵图更可靠——无间隙、无模糊、性能更好。

---

## 精灵图处理经验

### 背景去除

AI 生成的精灵图通常带有洋红色（magenta）或白色背景，需要预处理去除：

1. **洪泛填充法**：从图片四角和四边开始，递归去除连续的背景色像素
2. **颜色阈值法**：辅助去除残留的反锯齿边缘像素
3. **暗边缘清理**：多轮扫描，去除与透明像素相邻的深色像素（解决黑色描边问题）
4. **残影像素清理**：去除 alpha < 60 的半透明像素（洋红色去除后的残留）
5. **颜色特征清除**：基于 RGB 特征直接识别并去除洋红色残留像素（洪泛填无法到达的封闭区域）

```javascript
// 洪泛填充去除背景的核心思路
const queue = [边缘像素];
while (queue.length) {
    const pixel = queue.shift();
    if (是背景色) {
        设为透明;
        将相邻像素加入队列;
    }
}

// 颜色特征清除 —— 解决洪泛填充无法到达的"封闭区域"洋红色残留
for (const pixel of allPixels) {
    const { r, g, b } = pixel;
    // 洋红色特征：高红、低绿、中等蓝
    if (r > 100 && g < 80 && b > 60 && b > g * 1.2) {
        pixel.alpha = 0;
    }
}
```

**关键经验**：洪泛填充从边缘开始，但如果角色身体形成了"封闭区域"（如 Mario 走路时双腿之间的空隙），内部的洋红色背景像素无法被洪泛到达。这些残留像素颜色如 `rgb(184,36,111)` 或 `rgb(201,45,131)`，在游戏中表现为角色脚部的暗色线条或阴影。**必须额外做一轮基于颜色特征的全图扫描来清除这些漏网之鱼。**

### 图片缩放

- 使用 `fit: 'fill'` 而非 `fit: 'contain'`，后者会保留透明内边距导致瓦片间隙
- 小尺寸精灵图（如 16x20 金币）放大后模糊，不如直接用 Canvas 绘制

### 管道精灵图

- 管道顶部生成为 64x64（覆盖 2×2 瓦片区域）
- 管道身体不用精灵图，用 Canvas 画绿色矩形（避免每节都画一个完整管道）
- 需要去除白色背景（洪泛填充清理 3259 个背景像素）

---

## 关卡布局设计

### 地面系统

- 地面基础占 3 行（row 12-14），`fillRow11Between()` 在管道间隙等区域的 row 11 也填充地面瓦片
- **地表检测用动态判断，不用硬编码行号**：检查当前瓦片上方是否为空（`levelMap[row-1][col] === 0`），为空则为地表，渲染绿色草地+泥土；否则渲染纯棕色泥土
- 这样无论是 row 12 的正常地面还是 row 11 的填土地面，顶层都正确显示草地

```javascript
// 动态地表检测 —— 适用于任何行号的填土场景
case 1:
    if (row === 0 || levelMap[row - 1][col] === 0) {
        // 地表：绿色草地 + 草纹理 + 棕色泥土
    } else {
        // 地下：纯棕色泥土 + 纹理
    }
```

### 管道设计

管道由 4 种瓦片类型组成：

```
类型4(左上)  类型5(右上)   ← 管道顶部，绘制 T×2 大小的帽子
类型6(左体)  类型7(右体)   ← 管道身体，画绿色矩形
类型6(左体)  类型7(右体)   ← 可叠加多行
```

- 类型 5（右上）不绘制任何东西，因为类型 4 已经画了 T×2 宽度
- 身体部分用深浅不同的绿色矩形模拟光照效果

### 问号箱与金币

- 问号箱被顶后变为已用箱（类型 3 → 类型 8），同时更新 levelMap
- 金币不从箱子"预放置"，而是被顶时动态弹出
- 弹出金币使用 `isPopCoin` 模式：有重力、初速度、生命周期、透明度渐隐
- 弹出金币不可被再次收集（避免重复计分）

---

## 动画系统

### 拔旗动画（FLAGPOLE 状态）

分阶段状态机实现：

```
触碰旗杆 → slide（滑下）→ walk（走向城堡）→ done（显示通关）
```

1. **slide 阶段**：Mario 固定在旗杆 x 坐标，y 每帧 +3px 向下滑；旗帜同步下滑
2. **walk 阶段**：Mario 朝右每帧 +2px 走向城堡入口
3. **done 阶段**：显示 LEVEL CLEAR 覆盖层

**关键点**：
- 动画期间禁用键盘输入（不调用 `player.update(keys, tiles)`）
- 相机仍然跟随（调用 `updateCamera()`）
- 旗帜位置通过参数传递给渲染器，实现动态下滑效果

### 问号箱脉冲动画

```javascript
const pulse = Math.sin(this.animTimer * 0.08) * 0.1 + 0.9;
this.ctx.fillStyle = `rgb(${Math.floor(255 * pulse)}, ${Math.floor(200 * pulse)}, 0)`;
```

### 金币旋转效果

```javascript
const scaleX = Math.cos(this.animTimer * 0.15);
this.ctx.scale(scaleX, 1);  // 水平缩放模拟3D旋转
```

---

## 开始界面设计

- 标题界面使用 CSS 实现（非 Canvas），包含动画浮动的方块装饰
- 草地和泥土通过 `::before` 和 `::after` 伪元素绘制
- 支持点击按钮或按 Enter/空格键开始游戏
- 游戏在点击 Start 前不启动 gameLoop（节省资源）

---

## 测试方法

使用 Playwright 进行自动化视觉测试：

```javascript
const browser = await chromium.launch();
const page = await browser.newPage({ viewport: { width: 1280, height: 720 } });
await page.goto('http://localhost:8080');

// 键盘操作用 down/up 而非 press，避免按下太快不触发
await page.keyboard.down('Space');
await page.waitForTimeout(200);
await page.keyboard.up('Space');

// 远距离测试可用 evaluate 传送
await page.evaluate(() => {
    game.player.x = 153 * CONFIG.TILE_SIZE;
    game.cameraX = 150 * CONFIG.TILE_SIZE;
});

await page.screenshot({ path: 'test.png' });
```

**经验**：
- `keyboard.press()` 太快（keydown+keyup 同一帧），跳跃可能不触发，应改用 `keyboard.down()` + `waitForTimeout` + `keyboard.up()`
- 远距离关卡测试用 `page.evaluate()` 直接修改玩家坐标，避免等待过久

---

## Level 3：天空运动关（World 1-3）

### 主题与布局

Level 3 采用天空主题，背景为浅蓝色天空 + 白云，地面稀少，主要依靠树冠平台和木桥通行。关卡分为 7 段：

1. **起点安全区**：地面 + 少量金币，让玩家适应
2. **树冠跳跃段**：连续树冠平台，间距需要精准跳跃
3. **木桥段**：长木桥跨越深坑，桥面可踩踏
4. **移动平台段**：水平/垂直移动平台，需要把握时机
5. **掉落平台段**：踩上后开始震动，数帧后掉落
6. **弹簧板段**：弹簧板弹射到高处，获取隐藏金币
7. **终点冲刺**：回到地面，跑向旗杆

### 新增瓦片类型

| 类型 | 含义 | 渲染方式 |
|------|------|---------|
| 16 | 树冠 | 深绿色圆角矩形 + 树叶纹理 |
| 17 | 桥板 | 棕色木板 + 深色木纹 |

### 平台系统

三类平台各有独立的状态机：

**移动平台（Moving Platform）**
- 水平或垂直往返运动
- 玩家站在平台上时，随平台一起移动（deltaX/deltaY 累加到玩家坐标）
- 更新顺序：先更新平台位置，再处理玩家碰撞

**掉落平台（Falling Platform）**
```
idle → shaking (30帧) → falling → gone
```
- 玩家踩上后开始计时震动
- 震动期间平台左右小幅偏移（视觉反馈）
- 30帧后开始下落，加速度模拟重力
- 离开屏幕后标记为 gone，不再参与碰撞和渲染

**弹簧板（Springboard）**
- 默认状态：未压缩
- 玩家踩上后压缩（`compressed = true`）
- 压缩状态给玩家施加向上初速度（约 -18）
- 松开后恢复原状

### 新增敌人类型

**Paratroopa（飞行乌龟）**
- 上下浮动运动，浮动幅度约 ±30px
- 被踩后变成普通 Koopa（掉落地面，开始行走）
- 渲染时翅膀扇动（通过帧数切换翅膀位置）

**HammerBro（锤子兄弟）**
- 站在高台上，每隔 90 帧投掷一把锤子
- 锤子抛物线运动：初速度向上 + 水平向玩家方向
- 玩家被锤子击中 = 受伤（与敌人碰撞相同逻辑）
- 渲染时手臂摆动（投掷动作）

### 音频系统（Web Audio API）

**BGM 设计**
- 三关卡各有独立 BGM：overworld（地上）、underground（地下）、athletic（天空）
- 使用方波振荡器模拟 FC 音色
- 循环节奏基于 BPM：overworld 120bpm、underground 90bpm、athletic 180bpm
- 循环实现：`setTimeout` 在曲末重新调用 `play*()` 方法

**SFX 设计**
- 跳跃：三角波 300→600Hz，持续 0.15s
- 金币：方波 988Hz（B5）→ 1319Hz（E6），持续 0.1s
- 踩敌：噪声缓冲 + 低通滤波器，持续 0.1s
- 弹簧：锯齿波 200→900Hz，持续 0.3s
- 死亡：方波下行音阶 494→440→370→330→262Hz，每音 0.18s

**暂停/恢复**
- 暂停：`AudioContext.suspend()` 冻结所有音频
- 恢复：`AudioContext.resume()` + 重新计算循环计时器

### 死亡动画

FC 原版死亡动画：Mario 直立、双臂举起、身体僵直上飞后下落。实现要点：
- `!player.alive` 时渲染特殊死亡姿势（非跳跃帧）
- 死亡瞬间播放 `music.die()` BGM
- 所有死亡路径（敌人碰撞、坠崖、时间耗尽）必须调用 `music.stop(); music.die();`

---

## 踩坑记录

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 瓦片间有黑色网格线 | AI 精灵图自带黑色描边 | 改用 Canvas 代码绘制瓦片 |
| 问号箱被顶后外观不变 | 只更新了 tiles 对象，未更新 levelMap | 同时更新 `levelMap[row][col] = 8` |
| 地面显示三层草地 | row 13-14 也用了草地精灵图 | row > 12 只画棕色泥土 |
| 管道旁出现双层绿色草地 | `fillRow11Between()` 在 row 11 填充地面，`row <= 12` 导致 row 11 和 12 都渲染草地 | 改用动态地表检测：`levelMap[row-1][col] === 0` 判断是否为地表 |
| 金币预放在箱子上方 | 缺少弹出动画 | 改为动态生成 isPopCoin 弹出金币 |
| 拔旗动画不执行 | FLAGPOLE 代码在 `state !== PLAYING` return 之后 | 把 FLAGPOLE 处理移到 return 之前 |
| 管道显示为叠在一起的小管道 | 每个瓦片都画了完整管道精灵图 | 顶部画 T×2 大小，身体用矩形 |
| 弹出金币太小 | 精灵图只有 16×20 像素 | 放大到 24×30 并改用 Canvas 绘制 |
| Mario 脚前有黑线 | 角色双腿间形成封闭区域，洪泛填充无法到达，洋红色背景残留（如 `rgb(184,36,111)`） | 基于颜色特征全图扫描：`r>100 && g<80 && b>60 && b>g*1.2` 的像素设为透明 |
| 精灵图放大后模糊 | 原始尺寸太小 | 简单图形用 Canvas 绘制，复杂角色才用精灵图 |
| Level 3 运行 1.5s 后角色消失 | `playerBounds` const TDZ：hammer 碰撞循环引用了尚未声明的 playerBounds | 将 `const playerBounds = this.player.getBounds();` 移到 enemies.filter 之前 |
| 死亡时没有死亡音效 | 坠崖死亡路径只设置了 alive=false，未调用 music.die() | 所有死亡路径统一调用 `music.stop(); music.die();` |
| 暂停后 BGM 循环错位 | 暂停期间 setTimeout 仍在计数，恢复后循环计时器已过期 | 暂停时记录 `loopScheduledAt`，恢复时重新计算剩余时间 |

---

## 设计原则总结

1. **简单图形优先用代码绘制**：方形瓦片、圆形金币等用 Canvas 绘制比精灵图更干净、更可控
2. **精灵图需要预处理流水线**：去背景 → 去描边 → 去残影 → 颜色特征清除 → 缩放，每一步都可能影响最终效果
3. **数据层和渲染层必须同步**：修改瓦片状态时，所有引用该数据的地方都要更新
4. **状态机代码要注意执行顺序**：新增状态时，检查是否被早期 return 拦截
5. **自动化视觉测试不可少**：Playwright 截图是验证渲染效果的唯一可靠方式
6. **动画用分阶段状态机**：每个阶段有明确的完成条件，避免用 setTimeout 控制复杂序列
7. **精灵图清理不能只靠洪泛填充**：角色身体形成的封闭区域（如双腿之间）会保留背景色，必须额外做基于颜色特征的全图扫描
8. **渲染判断用动态检测，不用硬编码行号**：地表、水面等需要根据周围环境动态判断（如"上方是否为空"），硬编码行号在填土、挖洞等操作后会导致渲染错误
9. **平台系统需要先更新后碰撞**：移动平台必须先更新自身位置，再处理玩家碰撞；否则玩家会站在平台上一帧前的位置
10. **暂停/恢复必须处理音频计时器**：`AudioContext.suspend()` 冻结音频，但 JavaScript 的 `setTimeout` 仍在计数；恢复时必须重新计算循环计时器的剩余时间
11. **变量声明顺序决定可用性**：`const` 声明存在 TDZ（暂时性死区），在声明前访问会报错；多个循环共用变量时，声明必须放在所有使用点之前
12. **死亡路径必须统一处理**：敌人碰撞、坠崖、时间耗尽等所有死亡路径，必须调用相同的清理逻辑（停止 BGM、播放死亡音效、设置状态），避免遗漏导致"无声死亡"
